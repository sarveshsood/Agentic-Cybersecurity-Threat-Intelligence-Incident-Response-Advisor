# MITRE ATT&CK

ACTIRA **consumes** ATT&CK as an analyst ontology (mapping + heatmap + KB), not as a detection coverage product.

- Techniques inferred heuristically (`attack_mapping.py`)
- Catalog for UX (`attack_catalog.py`)
- Do not claim enterprise detection coverage percentages

See also product IR flow diagram `diagrams/12-incident-response.mmd`.
