# ACTIRA test / CI tooling (install alongside backend/requirements.txt)
#   pip install -r backend/requirements.txt -r requirements-test.txt

# Core pytest stack
pytest>=8.0.0
pytest-cov>=5.0.0
pytest-xdist>=3.6.0
pytest-mock>=3.14.0
pytest-asyncio>=0.24.0
pytest-timeout>=2.3.0
pytest-html>=4.1.0
pytest-env>=1.1.0
pytest-randomly>=3.15.0
pytest-playwright>=0.5.0

# HTTP / ASGI clients
httpx>=0.27.0
respx>=0.21.0
responses>=0.25.0

# Factories & time
factory-boy>=3.3.0
faker>=24.0.0
freezegun>=1.5.0

# Static analysis / format (also used by pre-commit + make lint)
black>=24.1.1
isort>=5.13.2
ruff>=0.6.0
flake8>=7.0.0
mypy>=1.8.0
types-requests>=2.31.0
types-PyYAML>=6.0.0

# Security / dependency scan (CI security.yml)
bandit>=1.7.9
pip-audit>=2.7.0
safety>=3.2.0

# Optional: selenium only if PLAYWRIGHT is unavailable; prefer Playwright
# selenium>=4.20.0
