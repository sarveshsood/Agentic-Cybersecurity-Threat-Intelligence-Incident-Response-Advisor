"""Core infrastructure (database, shared services).

P1 note: prefer ``backend.database``, ``backend.services``, and ``backend.security``
for new code. This package remains stable for lifespan and legacy imports.
"""
