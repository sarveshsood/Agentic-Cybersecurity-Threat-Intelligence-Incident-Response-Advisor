apiVersion: v1
kind: Service
metadata:
  name: { { include "actira.fullname" . } }
spec:
  type: { { .Values.service.type } }
  selector:
    app.kubernetes.io/name: actira
  ports:
    - port: { { .Values.service.port } }
      targetPort: { { .Values.service.port } }
