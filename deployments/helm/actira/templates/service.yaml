apiVersion: v1
kind: Service
metadata:
  name: {{ include "actira.fullname" . }}
  labels:
    {{- include "actira.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "actira.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
